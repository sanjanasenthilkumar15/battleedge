# WeldSense — EV Battery Cell Weld Inspection System
# Core dependencies (required)
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
Pillow>=10.0
opencv-python-headless>=4.8,<5.0
scikit-learn>=1.3
joblib>=1.3
pandas>=2.0
streamlit>=1.35
plotly>=5.18
statsmodels>=0.14.0

# Audio processing (optional — falls back to scipy STFT if missing)
librosa>=0.10

# XGBoost (for future risk model upgrade — optional for prototype)
# xgboost>=2.0

# Development / notebook utilities
# jupyter
# ipykernel
